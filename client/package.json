{
  "name": "penpals-client",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "del": "rimraf dist",
    "browser-sync": "browser-sync http://localhost:5000 --files 'dist' --no-notify --no-open",
    "autoprefixer": "postcss -u autoprefixer -r dist/css/**/*",
    "sass": "node-sass src/scss -o dist/css",
    "sass:build": "npm run sass --output-style compressed && npm run autoprefixer",
    "webpack:build": "webpack --mode production",
    "build": "npm run webpack:build && npm run sass:build",
    "sass:dev": "node-sass src/scss -o dist/css & node-sass src/scss -o dist/css --watch && npm run autoprefixer",
    "webpack:dev": "webpack --mode development --watch",
    "dev": "npm run del && npm run sass:dev & npm run webpack:dev & npm run browser-sync"
  },
  "author": "Brandon Patterson",
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.0",
    "@babel/preset-react": "^7.9.4",
    "autoprefixer": "^9.7.5",
    "babel-loader": "^8.1.0",
    "browser-sync": "^2.26.7",
    "node-sass": "^4.13.1",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^7.1.0",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
